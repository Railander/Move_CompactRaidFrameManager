CF_API_KEY.txt
*.key
*.pem
*.pfx
.env
.env.*
*token*
secret*

# local UI source dumps live at the workspace root, never inside a repo
source/

.codegraph/

# agent instructions and dev suites stay strictly local: the public repo
# holds only third-party-facing files, never agent docs or tests
AGENTS.md
AGENTS_MEMORY.md
tests/
